-- network player controler

local network = {}

function network.init_entity(self,keymap)
   -- TODO: init network controller data
end

function network.update()
   -- TODO: update network control
end

return network
